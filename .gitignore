# Local Claude Code config — must never be committed (contains Figma PAT)
.mcp.json
.mcp.local.json

# Gradle cache & build artifacts
.gradle/
**/.gradle/
app/build/
**/build/

# IntelliJ / Android Studio
# workspace.xml 已被旧 commit 跟踪,这里只对未来文件生效
# 移除跟踪需要团队协调(第二步),暂不 git rm --cached
.idea/
*.iml

# Python API local state
**/.venv/
**/__pycache__/
**/.pytest_cache/
**/.coverage
**/*.db
services/api/.env

# Generated local acceptance evidence and scratch output
artifacts/
output/
tmp/

# Root-level dev cruft (per ENVIRONMENT.md §8.3 — not project files)
clean_text.py
write_vertical_text.py
hs_err_*.log
replay_*.log

# Personal session notes (per conversation 2026-09-07)
# SESSION-LOG-*.md in docs/ SHOULD be committed (team history)
# but if you keep your own private notes elsewhere, ignore them
notes/
*.local.md

# ⚠️ 本地工具脚本:**整个 scripts/ 不入库**(2026-09-20 用户决定)
#   理由:它们要么绑设备(真机回归、截图小工具)、要么是我这边的辅助脚本,
#         属于开发辅助而不是产品代码。**文件留在本地、历史里也还在**。
#   取回:git log --oneline -- scripts/<名字>  →  git checkout <commit> -- scripts/<名字>
#   ⚠️ 判据与踩坑**不在这里,已固化进 docs/**(纯文档入库):
#     · docs/CHUANGDANG-REGRESSION-PLAYBOOK.md —— 闯荡江湖真机回归的判据/顺序/踩坑
#     · ONBOARDING §3.4 / §5.4 里给了 setup-reverse / audit-comment-drift 的等价命令
/scripts/
