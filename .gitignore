# Local Claude Code config — must never be committed (contains Figma PAT)
.mcp.json
.mcp.local.json

# Gradle cache & build artifacts
.gradle/
**/.gradle/
app/build/
**/build/

# Python API local state
**/.venv/
**/__pycache__/
**/.pytest_cache/
**/.coverage
**/*.db
services/api/.env
